import React from "react";
import { Tilt } from 'react-tilt';
import { motion } from "framer-motion";

import { styles } from "../styles";
import { services } from "../constants";
import { SectionWrapper } from "../hoc";
import { fadeIn, textVariant } from "../utils/motion";

const ServiceCard = ({ index, title, icon }) => (
  <Tilt className='xs:w-[250px] w-full'>
    <motion.div
      variants={fadeIn("right", "spring", index * 0.5, 0.75)}
      className='w-full green-pink-gradient p-[1px] rounded-[20px] shadow-card'
    >
      <div
        options={{
          max: 45,
          scale: 1,
          speed: 450,
        }}
        className='bg-tertiary rounded-[20px] py-5 px-12 min-h-[280px] flex justify-evenly items-center flex-col'
      >
        <img
          src={icon}
          alt='web-development'
          className='w-16 h-16 object-contain'
        />

        <h3 className='text-white text-[20px] font-bold text-center'>
          {title}
        </h3>
      </div>
    </motion.div>
  </Tilt>
);

const About = () => {
  return (
    <>
      <motion.div variants={textVariant()}>
        <p className={styles.sectionSubText}>Introduction</p>
        <h2 className={styles.sectionHeadText}>Overview.</h2>
      </motion.div>

      <motion.p
        variants={fadeIn("", "", 0.1, 1)}
        className='mt-4 text-secondary text-[17px] max-w-5xl leading-[30px]'
      >
        I'm a passionate computer science student with a keen interest in Software Development, Argument Reality,
        Virtual Reality,Artificial Intelligence (AI), Cloud Computing, and Machine Learning (ML). I'm constantly 
        immersing myself in the latest advancements in these areas, understanding how they're being leveraged to solve 
        complex real-world problems and drive innovation. I firmly believe that these technologies hold the 
        potential to revolutionize industries and enhance the quality of life globally. My technical expertise 
        spans across Java, Python, JavaScript, Generative AI, Microsoft Power platform, Figma, Source 
        Control, and Database management.I pride myself on being a quick learner and a collaborative team player, 
        focusing on creating efficient, scalable, and user-friendly solutions that address real-world challenges. 
        In the future, I aspire to utilize my knowledge and skills to devise innovative solutions that have a positive 
        and lasting impact on society.
      </motion.p>

      <div className='mt-20 flex flex-wrap gap-10'>
        {services.map((service, index) => (
          <ServiceCard key={service.title} index={index} {...service} />
        ))}
      </div>
    </>
  );
};

export default SectionWrapper(About, "about");